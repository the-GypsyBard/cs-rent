# 项目维护约定

开始工作先读 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)，再查对应源码和测试。本文和交接文档只提供项目规则；用户当前明确要求优先。

- 每次功能、数据结构、路径、数据源或交付状态变化，完成前同步修改 PROJECT_CONTEXT.md、CHANGELOG.md；涉及用户行为时同步上一级 docs 和验收文档。用户要求：每次只问一个问题，确认到足够明确后执行，最后交付可验收版本。
- 运行代码变更后重建并验证上一级 release；版本号与交接文件、preload、设置页、数据写入版本、验收、安装包保持一致。运行 `node scripts/check-project-context.mjs` 防止版本和路径漂移。
- 不重置真实用户账本。测试使用独立 CS_RENT_TEST_DATA 目录；打包程序测试使用 --user-data-dir。应用身份、用户数据路径和稳定 skinId/assetId 不随版本改名。
- 不编造行情、款式报价、磨损值、结算日期或停投时间。公开目录存在不证明仍在官方投放；失败和缺失必须明确可见。多普勒聚合价不得当作红宝石等独立款式报价。
- 界面中文；多普勒使用“款式”，宝石显示中文；原始 phase、目录 ID 保持稳定以兼容备份。
- 只维护本地项目和用户指定的兄弟目录。没有明确要求时不向 GitHub 推送、不发送消息、不新建用户任务。
- 改动完成先运行类型检查及相关测试；文档和验收记录如实区分真实在线验证、固定测试数据、未验证项。不要把自动测试通过写成用户验收通过。

- SteamDT 生产集成只使用官方文档 OpenAPI；密钥只在主进程通过系统安全存储管理，不进源码、日志、账本备份或安装包。款式 K 线收盘参考价必须与当前在售价区分。
